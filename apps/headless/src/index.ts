// Headless runner: max-speed sim with no renderer — determinism checks + balance harness.
// The agent grows this into the balance tooling once Gate 3 lands.
console.log('headless runner — not yet implemented (see docs/build-plan.md)')
